import { CoolEffectDirective } from './cool-effect.directive';

describe('CoolEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new CoolEffectDirective();
    expect(directive).toBeTruthy();
  });
});
